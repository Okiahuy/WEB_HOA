import React from 'react';
import TypeManager from '../../components/Admin/Type/TypeManager';

const TypeAdmin = () => {
    return (
        <div>
          <h1>Danh Sách Loại Sản Phẩm</h1>
          <TypeManager /> {/* Hiển thị danh sách sản phẩm */}
        </div>
      );
  };
  
  export default TypeAdmin; // Export mặc định